
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Règles ultra-simples pour que tout fonctionne
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
